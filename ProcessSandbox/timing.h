#include <stdint.h>

// Usage:
// Call startTimer() to begin timing and stopTimer() to stop;
//   the difference between the two return values indicates elapsed time
uint64_t startTimer();
uint64_t stopTimer();
